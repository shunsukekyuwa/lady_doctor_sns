<%= form_for([@male,@male_post]) do |f| %>
  <% if @male_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@male_post.errors.count, "error") %> prohibited this male_post from being saved:</h2>

      <ul>
      <% @male_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  
  <div class="hidden">
    <%= f.hidden_field :male_id, :value => @male.id %>
  </div>
  <div class="field">
    <%= f.label :image %>
　　 <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>